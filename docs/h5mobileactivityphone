基于现有“用户侧活动专区PC端HTML页面”，新增一套H5移动端适配版本页面。

要求：

1. 不修改任何业务逻辑
2. 不修改Mock数据
3. 不重构功能
5. 仅做UI适配与页面重排

新增内容：

- 生成一套独立H5页面（mobile版本HTML）
- 可以与现有PC端页面并存

适配要求：

1. 适配手机端
2. 使用响应式布局或flex布局

页面范围：

- 活动列表页（活动专区首页）
- 活动详情页
- 活动报名页
- 我的活动页

输出要求：

- 每个HTML文件写入到 mobimobile-Activity-zone/ 目录
- 每个页面独立HTML文件
- 直接可运行打开

核心原则：
复用PC端逻辑，只做移动端UI重排，不改功能。